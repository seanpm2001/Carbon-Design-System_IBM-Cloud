import React from 'react';

const inactiveIcon = (
  <div
    aria-hidden="true"
    style={{
      backgroundColor: 'grey',
      width: '14px',
      height: '14px',
      borderRadius: '50%',
    }}
  />
);

export default inactiveIcon;
