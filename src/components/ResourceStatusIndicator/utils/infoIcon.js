import React from 'react';

const infoIcon = (
  <div
    aria-hidden="true"
    style={{
      backgroundColor: '#0F62FE',
      width: '14px',
      height: '14px',
      borderRadius: '50%',
    }}
  />
);

export default infoIcon;
