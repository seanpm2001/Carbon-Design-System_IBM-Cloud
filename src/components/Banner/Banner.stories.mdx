import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { Button } from "@carbon/react";
import Banner from ".";
import "./_index.scss";

<Meta
  title="Components/Banner"
  component={Banner}
  argTypes={{
    locale: {
      control: { type: "select" },
      options: [
        "de",
        "en",
        "es",
        "fr",
        "it",
        "ja",
        "ko",
        "pt-br",
        "zh-cn",
        "zh-tw",
      ],
      defaultValue: "en",
    },
    children: { control: false },
  }}
  parameters={{ status: { type: "core" } }}
/>

# Banner

export const Template = (args) => (
  <Banner {...args}>
    Banner is a component used for important notifications that stretch across
    the entire width of the screen to draw the user's attention.
    <Button kind="primary" size="small">
      Button
    </Button>
  </Banner>
);

<Canvas>
  <Story
    name="Demo"
    args={{
      dismissible: true,
      withGradient: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Description

Banner is a component used for important notifications that stretch across the entire width of the screen to draw the user's attention. Buttons rendered as children of this component will be colored to contrast appropriately with the gradient background.

## Usage

1.  Install the [@console/pal](https://github.ibm.com/ibmcloud/pal#how-to-install-and-use) library
2.  Import the styles:

    ```js
    @import '@console/pal/Components/Banner/styles';
    ```

3.  Import the component:

    ```js
    import { Banner } from "@console/pal/Components";
    ```

4.  Render the component.

For general component usage review the [Components Docs](https://pages.github.ibm.com/ibmcloud/pal/development-guidelines/components/overview).

## Props

<ArgsTable of={Banner} />

## Loading State

<Canvas>
  <Story name="Skeleton">
    <Banner.skeleton />
  </Story>
</Canvas>

## Contributors

- Aaron Baker
- Tony Wu
